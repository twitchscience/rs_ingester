# Upstart script at /etc/init/dbstorer.conf

start on runlevel [2345] and stopped cloud-final
stop on runlevel [06]

kill signal SIGINT
kill timeout 600

respawn
respawn limit 10 5

script
  # use su <user> because that makes /etc/environment variables available to subprocess
  exec su root -c /opt/science/rs_ingester/bin/run_dbstorer.sh
  emit storer_running
end script
