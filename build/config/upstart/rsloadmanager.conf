# Upstart script at /etc/init/rsloadmanager.conf

start on runlevel [2345] and stopped cloud-final
stop on runlevel [06]

kill signal SIGINT
kill timeout 600

respawn
respawn limit 10 5

script
  exec /opt/science/rs_ingester/bin/run_rsloadmanager.sh
  emit ingester_running
end script
